# 常见问题排查

本章是中文摘要。完整排查步骤以 [../TROUBLESHOOTING.md](../TROUBLESHOOTING.md) 为准。

## 安装失败

如果出现：

```text
Existing repo has local modifications
```

说明服务器部署目录有本地修改。先检查：

```bash
git -C /var/www/terraf_shell status --short
```

确认可以丢弃后再执行：

```bash
sudo env RESET_WORKTREE=1 RUN_SEED=0 bash auto_deploy.sh your-domain-or-ip
```

## 前端不能访问

```bash
sudo systemctl status terraf-frontend
sudo journalctl -u terraf-frontend -f
curl -I http://127.0.0.1:3000/
```

确认 `B2C_frontend/.env.local` 包含：

```dotenv
NEXT_PUBLIC_API_BASE_URL=/api
NEXT_SERVER_API_BASE_URL=http://127.0.0.1:8000/api
API_PROXY_TARGET=http://127.0.0.1:8000
```

## API 或后台不能访问

```bash
curl -I http://127.0.0.1:8000/up
sudo systemctl status php8.3-fpm
sudo nginx -t
sudo tail -f /var/log/nginx/error.log
```

后台默认地址：

```text
http://your-domain-or-ip:8000/admin
```

## 图片不显示

本地 storage：

```bash
cd /var/www/terraf_shell/B2C_backend
php artisan storage:link
ls -l public/storage
```

Azure storage：

- 在后台 Storage Settings 执行连接测试。
- 执行上传测试。
- 检查 container、key、public URL 和 SAS TTL。

## 购物车 / Checkout 问题

检查：

- 商品和变体是否启用。
- SKU 库存和库存策略。
- Guest Checkout Feature Flag。
- Shipping Settings 是否能返回配送选项。
- Tax Settings 是否正确。
- `B2C_backend/storage/logs/laravel.log`。

## GST / Shipping 不生效

后台保存 Tax Settings 或 Shipping Settings 后清缓存：

```bash
cd /var/www/terraf_shell/B2C_backend
php artisan optimize:clear
```

NZ Post 报价需要后台 NZ Post Settings 凭据可用；否则 `auto` 会回退到手动费率。

## 500 / 502 / 404

- 500：查看 Laravel 日志，重点检查 `.env`、APP_KEY、数据库、storage 权限。
- 502：检查 `terraf-frontend`、Laravel `/up`、PHP-FPM、Nginx upstream。
- 404：检查 Nginx 站点、Next 路由、Laravel route cache。

## 队列和调度器

```bash
sudo supervisorctl status terraf-queue:*
tail -f /var/www/terraf_shell/B2C_backend/storage/logs/queue-worker.log
cat /etc/cron.d/terraf-scheduler
sudo systemctl status cron
```
